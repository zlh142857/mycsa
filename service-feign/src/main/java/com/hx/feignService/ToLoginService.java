package com.hx.feignService;/*
 */

public interface ToLoginService {
    //String ajaxLogin(@RequestParam("name")String name, @RequestParam("password")String password, @RequestParam(value = "rememberMe",required = false) boolean rememberMe);
}
